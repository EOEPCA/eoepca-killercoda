This scenario prepares the environment for the Sentinel-5P cloud fraction notebook:

- **IAM** authenticates users and supplies tokens for the service APIs.
- **Workspace** provides S3 storage for prepared STAC metadata and results.
- **Data Access** provides the STAC API, Browser and Manager.
- A **Python virtual environment** supplies the notebook's data access and plotting libraries.
