The tutorial uses the `eoepca` bucket in the supplied MinIO instance for processing outputs.

Check that the bucket exists:

```
mc ls minio-local
```{{exec}}

The S3 endpoint and credentials are already configured in `~/.eoepca/state`.
